func int main = () => {
        
        array[str, int] num;
        num["one"] = 0;
        num["other"] = 1;
        num["else"] = 2;

        array[int, str] wrd;
        wrd[1] = "Hello";
        wrd[2] = "World!";

        array[int, str] wrd2;
        wrd[1] = "other";
        wrd[2] = "else";

        int x = num["other"]; 
        int y = num["else"];

        sprint(wrd[num[wrd2[x]]]); 
        sprint(wrd[num[wrd2[y]]]); 
        return 0;
}